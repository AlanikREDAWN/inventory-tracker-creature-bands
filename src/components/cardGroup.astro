---
const { result } = Astro.props;
---
<!-- <div class="grid grid-cols-3 gap-4 content-center place-items-center text-center">
	{
		result.map((item) => (
			<div class="m-15 bg-amber-100 p-20 rounded-3xl border-amber-950 border-3 transition duration-500 hover:scale-105">
				<h1 class="text-xl text-heading font-semibold tracking-tight">{item.productName}</h1>
				<br>
				<div class="flex content-normal gap-10 justify-between">
					<p class="text-3xl font-extrabold text-heading">{item.price} </p>
					<br>
					<p class="font-bold">Amount Left: {item.amount}</p>
				</div>
			</div>
		))
	}
</div> -->

<div class="grid grid-cols-3 gap-4 content-center place-items-center text-center">
	{
		result.map((item) => (
			<div class="m-15 bg-indigo-200 p-20 rounded-3xl border-indigo-700 border-3 transition duration-500 hover:scale-105">
				<h1 class="font-body text-2xl text-heading font-semibold tracking-tight text-indigo-800">{item.productName}</h1>
				<br>
				<div class="font-body flex content-normal gap-10 justify-between">
					<p class="text-4xl font-extrabold text-heading text-indigo-700">{item.price} </p>
					<br>
					<p class="font-bold text-xl text-indigo-700">Amount Left: {item.amount}</p>
				</div>
			</div>
		))
	}
</div>